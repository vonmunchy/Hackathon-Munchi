// Package webhooks implements the `swipe webhooks` CLI tree (Phase 5):
// `listen` (local receiver + signature verifier + optional forwarder),
// `verify` (offline file-based signature check), `secret` (read the
// running mock's webhook secret).
package webhooks
