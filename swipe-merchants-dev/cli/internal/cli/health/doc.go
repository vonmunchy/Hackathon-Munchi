// Package health implements the `swipe health` subtree which probes the
// running mock's /health/alive and /health/ready endpoints and renders
// the result in table/json/yaml format.
package health
