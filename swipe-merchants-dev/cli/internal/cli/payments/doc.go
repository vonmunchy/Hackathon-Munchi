// Package payments implements the `swipe payments` CLI subtree. Phase 3
// adds `swipe payments get <id>` (read against /api/v1/payments/{id}).
// `create` and `watch` land in Phase 4.
package payments
