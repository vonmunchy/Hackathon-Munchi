// Package logs implements the `swipe logs` CLI subtree (Phase 3). It hits
// the localhost-only /_admin/logs surface so devs can inspect the mock's
// request history without leaving the terminal.
package logs
