// Package cli is the root of the cobra-based command tree. The root command
// owns global flags (--output, --quiet, --verbose, --no-color, --port,
// --config). Each subcommand lives in its own subpackage.
package cli
