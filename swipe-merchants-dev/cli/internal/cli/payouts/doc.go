// Package payouts implements the `swipe payouts` CLI subtree (Phase 4).
// It calls POST /api/v1/payouts to initiate withdrawals from the merchant
// wallet to a linked bank account.
package payouts
