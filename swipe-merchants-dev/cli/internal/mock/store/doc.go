// Package store is the BoltDB-backed persistence layer for the embedded
// mock server. Phase 1 covers merchants, wallets, bank accounts, and meta;
// later phases extend the bucket set to cover clients, payments, payouts,
// transactions, scenarios, and the request log.
package store
