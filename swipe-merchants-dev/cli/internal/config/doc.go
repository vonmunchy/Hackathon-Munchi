// Package config holds the resolved file-system locations the swipe binary
// reads from and writes to (~/.swipe and its subtree). Phase 1 only needs
// path resolution; merchant-facing config keys land in later phases.
package config
